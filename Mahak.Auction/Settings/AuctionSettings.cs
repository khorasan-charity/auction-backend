namespace Mahak.Auction.Settings;

public static class AuctionSettings
{
    private const string Prefix = "Mahak.Auction";
    
    public const string GlobalSettings = Prefix + "." + nameof(GlobalSettings);
}
